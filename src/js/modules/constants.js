// ===== CONSTANTS ===== //

// налаштування проєкту та інформація про середовище
export const CONFIG = {
  PROJECT_NAME: 'U1Core',
  USER_AGENT: navigator.userAgent,
};

// селектори DOM
export const SELECTORS = {
  ROOT: document.documentElement,
  BODY: document.body,
};

// імена CSS-класів
export const CLASSES = {
  ACTIVE: '_active',
  HIDDEN: '_hidden',
  SHOW: '_show',
  LOADED: '_loaded',
  COLLAPSED: '_collapsed',
};
